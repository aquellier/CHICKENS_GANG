<div class="container">

<h1>Available Chickens Gangs</h1>

<p> Please click on a Chicken Gang for more information.</p>

  <div class="row">
    <% @chickens_gangs.each do |chickensgang| %>
    <div class="col-xs-12 col-sm-4">

        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_tag chickensgang.photo, height: 200 %>')">


          <div class="card-category"><%= chickensgang.breed %></div>
          <div class="card-description">
            <h2><%= chickensgang.gang_name %></h2>
            <p>Capacity: <%= chickensgang.capacity %></p>
            <p>Price: <%= chickensgang.price %></p>
          </div>
          <img class="card-user" src="https://kitt.lewagon.com/placeholder/users/tgenaitay">
          <div class="link">
            <%= link_to 'View more information', chickens_gang_path(chickensgang) %>
          </div>
        </div>
      </div>
      <% end %>
    </div>

    <button>
      <%= link_to "Add a new chicken gang",new_chickens_gang_path %>
    </button>
  </div>
